'use strict'

@TextboxCtrl = @controllerModule.controller "TextboxCtrl", ($scope, $state, $stateParams) ->
  # $scope.showSubmitButton = true
  # $scope.showNextButton   = false
  # $scope.$emit 'textBox:init'

@TextboxCtrl.$inject = ['$scope', '$state', '$stateParams']
