'use strict';

angular.module('painSquad.controllers')

.controller('LoginCtrl', ['$scope, $location', 'apiService', 'storageService'],
  function($scope, $location, apiService, storageService) {
  // code goes hurr
});
