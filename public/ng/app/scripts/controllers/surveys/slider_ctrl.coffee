'use strict'

@controllerModule.controller "SliderCtrl", ['$scope', '$state', ($scope, $state) ->
  # $scope.currentForm.slider.$valid = false
  # $scope.currentForm.slider.$setValidity('required', false)

]
